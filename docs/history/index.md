# Historical Documents

This directory preserves completed plans and specifications. References to
older package names, file paths, or UI class names describe the repository at
the time the document was written; they are not current implementation
instructions.

Use `../architecture/current.md` and `../architecture/structure.md` for the
current codebase.
